# Specs Directory

Canonical feature specifications live in [`docs/specs/`](../docs/specs).
This directory exists to keep the repository structure aligned with the KoppaJS standard without duplicating the actual spec source.
